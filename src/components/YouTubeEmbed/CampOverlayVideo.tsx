import React from "react"

import {Box} from "@mui/material"

type CampOverlayVideoProps = {
  url: string
}

export default function YouTubeEmbed({
  url,
}: CampOverlayVideoProps) {
  const ref = React.useRef<HTMLDivElement>()
  return (
    <Box>
    <YouTubeEmbed
    />
    </Box>
  )
}
